import { Bebas_Neue, Dosis } from 'next/font/google';

export const dosisSemibold = Dosis({subsets: ['latin'], weight: ['600']});
export const bebasNeue = Bebas_Neue({ subsets: ['latin'], weight: ['400'] });